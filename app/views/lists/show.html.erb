<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h3>Due within 3 days...</h3>
      <ul>
        <li>task</li>
        <li>task</li>
        <li>task</li>
      </ul>
    </div>
    <div class="col-md-9">
      <h2>Task</h2>
      <% if current_user %>
      <table class="table table-striped table-hover">
        <th>Task Name</th>
        <th>Days Left</th>
        <% @lists_tasks.each do |task| %>
        <tr>
          <td><%= task.name %></td>
          <td><%= task.created_at %></td>
        </tr>
        <% end %>
      </table>
        <%= render partial: 'addtask' %>
      <% else %>
        <p>
          You need to
          <%= link_to "Sign In", new_user_session_path %>
          to view your list(s).
        </p>
      <% end %>
    </div>

    </div>
</div>
